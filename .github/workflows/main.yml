name: first wf
on: workflow_dispatch
jobs:
  job1:
      runs-on: ubuntu-latest
      steps:
        - name: install php
          run: |
            sudo apt update
            sudo apt install php -y
            php --version
